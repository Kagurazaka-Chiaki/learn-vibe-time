# Ready Batches

Place runnable batch files here.

Each batch must list explicit task paths, allowed commands, forbidden commands,
and exit conditions. The agent must only execute the active batch listed in
`.agent/STATE.md`.
